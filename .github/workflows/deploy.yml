name: Deploy Supabase

on:
  push:
    branches:
      - develop
      - main
    paths:
      - "supabase/**"
      - ".github/workflows/deploy.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: supabase-${{ github.ref_name }}
  cancel-in-progress: false

jobs:
  database-test:
    name: Database tests
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Supabase CLI
        uses: supabase/setup-cli@v2
        with:
          version: 2.109.1

      - name: Start local database
        run: supabase db start

      - name: Apply migrations from scratch
        run: supabase db reset

      - name: Run pgTAP tests
        run: supabase test db

  deploy:
    name: Deploy to Supabase
    needs:
      - database-test
    if: github.ref_name == 'develop' || github.ref_name == 'main'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    environment:
      name: ${{ github.ref_name == 'main' && 'prd' || 'stg' }}

    env:
      SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
      SUPABASE_DB_PASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
      SUPABASE_PROJECT_ID: ${{ vars.SUPABASE_PROJECT_ID }}

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Supabase CLI
        uses: supabase/setup-cli@v2
        with:
          version: 2.109.1

      - name: Link Supabase project
        run: supabase link --project-ref "$SUPABASE_PROJECT_ID"

      - name: Preview pending migrations
        run: supabase db push --dry-run

      - name: Deploy pending migrations
        run: supabase db push

      # Edge Functions는 마이그레이션과 별개로 배포된다.
      # 0014_webhook 트리거가 부르는 send-email 함수가 없으면 알림이 실제로 나가지 않는다.
      # 실발신 시크릿(EMAIL_API_KEY 등)은 프로젝트에 별도 설정 — 미설정 시 함수는 dry-run(무해).
      - name: Deploy Edge Functions
        run: supabase functions deploy --project-ref "$SUPABASE_PROJECT_ID"
